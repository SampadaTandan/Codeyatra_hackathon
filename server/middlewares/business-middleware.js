// const validateBusinessData = (req, res, next) => {
//     const { profitMargin } = req.body;
//     if (profitMargin < 0 || profitMargin > 100) {
//         return res.status(400).json({ error: 'Profit margin must be between 0 and 100' });
//     }
//     next();
// };

// module.exports = { validateBusinessData };
